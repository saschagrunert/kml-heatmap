# Prettier reads .gitignore as well, so this only lists tracked files that
# must keep their own formatting
*.lock
# A string.Template with $placeholders, not valid JavaScript
kml_heatmap/templates/map_config_template.js
